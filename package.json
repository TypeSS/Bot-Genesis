{
  "name": "bot-genesis",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm -r dev",
    "bot:dev": "pnpm --filter @genesis/bot dev",
    "dashboard:dev": "pnpm --filter @genesis/dashboard dev"
  }
}
